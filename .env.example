# Payments are handled by a Stripe Payment Link (no-code hosted checkout),
# so no secret keys, database, or webhooks are required to run this site.
#
# The Payment Link URL and ticket figures live in: src/lib/event.ts
#
# Optional app config (safe to omit — sensible defaults are used):
NEXT_PUBLIC_EVENT_DATE=2026-09-02
